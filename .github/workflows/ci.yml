name: CI - Nx Monorepo Checks

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Install Nx CLI globally
        run: npm install -g nx

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Test (optional)
        run: npx nx run-many -t test || echo "No tests found"

      - name: Verify
        run: echo "✅ Monorepo CI pipeline passed successfully!"
